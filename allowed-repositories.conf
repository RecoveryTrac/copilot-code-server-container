# Azure DevOps Repository Whitelist
# 
# Defines which Azure DevOps repositories agents are allowed to access.
# One repository name per line. Lines starting with # are ignored.
#
# Security: Repository names are validated to contain only alphanumeric
# characters, dots, dashes, and underscores to prevent injection attacks.
#
# To allow additional repositories: Add the repository name on a new line.
# After editing, rebuild the container: docker compose up -d --build

Aspire
Services
Mobile
DataHub
